# Global constant for prima vita environment

# Import required libraries
import pygame

# Set screen height and width
SCREEN_WIDTH = 1200
SCREEN_HEIGHT = 700

# Create pygame display instance
screen = pygame.display.set_mode((SCREEN_WIDTH, SCREEN_HEIGHT))
